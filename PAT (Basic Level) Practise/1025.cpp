#include <iostream>
#include <list>
struct node{
	int address;
	int data;
	int next;
}
void printLn(std::list ln&,int k){
	for_each(
int main()
{
	int first_address,n,k;
	std::cin>>first_address>>n>>k;
	auto ln = ReadNode(cin,n);
	reverseLn(ln,k);
	printLn(ln);
	return 0;
}
